        if(displayVal != "0"){
            /*
*
*/      QString newVal = displayVal + QString::fromStdString(ptr);



        newVal = newVal + butVal;
        double dblNewVal = newVal.toDouble();
        ui->Display->setText(QString::number(dblNewVal, 'g', 16));
        }/*displayVal equals zero.*/
        else {
          if(butVal == "0")
            {
                leadingZeros++;
            } else {
              for(int i = 0; i < leadingZeros; i++){
                  newVal = newVal + "0";
              }
              leadingZeros = 0;
          }

          QString newVal = displayVal + QString::fromStdString(ptr) + butVal;
          double dblNewVal = newVal.toDouble();
          ui->Display->setText(QString::number(dblNewVal, 'g', 16));
        }
